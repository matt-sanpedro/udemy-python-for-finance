"""
Docstring for p-06-finance-packages

NumPy: a third-party package allowing us to work with multidimensional arrays
Pandas (panel data): allows us to organize data in a tabular form and to attach descriptive 
    labels to the rows and columns of the table
Matplotlib: a 2d plotting library specifically designed for visualization of NumPy arrays
    a plotting library for creating static, animated, and interactive visualizations in Python
math: built-in package for mathmematical functions and constants
random: invokes random number generators
statsmodels: descriptive statistics, plotting functions, regressions
"""
import numpy as np
import pandas as pd
import matplotlib
import math
import random
import statsmodels
import pandas_datareader
